language: python
dist: xenial
before_install:
  - curl -sSL https://raw.githubusercontent.com/sdispater/poetry/master/get-poetry.py | python
  - source $HOME/.poetry/env
install:
  - poetry install
script:
  - poetry build
  - poetry config repositories.testpypi https://test.pypi.org/legacy/
  - poetry config http-basic.foo $PYPI_USER $PYPI_PASS
deploy:
  - provider: script
    skip_cleanup: true
    script: poetry publish
    on:
      branch: master
      python: '3.6'
#      tags: true